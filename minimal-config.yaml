# SingerOS Minimal Configuration (for quick startup)
# Uncomment database section when you have PostgreSQL setup

github:
  app_id: 0
  private_key: ""
  webhook_secret: ""
  client_id: ""
  client_secret: ""
  base_url: "https://api.github.com"
  skip_webhook_verify: true  # 开发模式下跳过webhook签名验证

# gitlab:
#   # Gitlab app configuration (will be implemented as needed)

nats:
  url: "nats://localhost:4222/"

# database:
#   driver: postgres
#   url: "host=localhost user=singer password=singer dbname=singer port=5432 sslmode=disable"
#   debug: false